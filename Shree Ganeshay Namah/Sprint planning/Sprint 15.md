# Sprint 15 — Local caching layer & real Categories tab

**Status: `proximity_app/` code complete, `flutter analyze`-clean (40 issues, all pre-existing-class info-level lints -- `prefer_initializing_formals`/`use_null_aware_elements`, 0 errors/0 warnings, same bar Sprint 13/14 held), real-device pass complete on the same motorola edge 70 used since Sprint 13.** The one piece of backend scope this sprint turned out to need (see "Scope check" below) is written and locally type-checked but **not yet deployed** -- blocked by this session's own tooling, not a code problem; see "One more step you'll need" below. Inserted after the original plan, same reason and shape as Sprint 14: "iOS build & store submission" bumped from Sprint 15 to **Sprint 16** in `SPRINT_PLANNING.md` (§3.1, §3.2, §11, §12, §13 cross-references all updated) rather than reusing its number.

## Why this sprint exists

Two gaps, both surfaced by actually using the app for the first time in this project's history (Sprint 13/14's real device testing), not from re-reading the original plan:

1. **No local caching layer, anywhere.** Every screen does a live network round-trip to Supabase on every load — this codebase's own comments have named this gap explicitly, repeatedly, since as early as Sprint 1 ("no Drift caching yet"), always as a deliberately-deferred concern for "a later sprint" that never got scheduled a number until now.
2. **The Categories tab is still Sprint 1's original placeholder.** `app_router.dart`'s `/categories` route still renders `PlaceholderScreen(title: 'Categories')` — literally "Categories -- coming soon". Home, Cart, and Order Again all graduated from this exact same placeholder in their own sprints (4, 6, 10); Categories never did, and nothing in any later sprint's scope ever mentioned it again.

## Decisions made before writing this (per your answers)

- **Caching scope: targeted, not full offline-first.** A full offline-first rebuild (every screen works offline, conflict resolution, sync queues) is a much larger undertaking than one sprint should carry, and this app doesn't need it — it needs the *perceived* speed of not re-fetching the same nearby-shops list every time a buyer taps back into Home. Cache-then-revalidate on read-heavy, high-traffic screens only.
- **iOS bumped to Sprint 16**, not merged or renumbered differently — matches the precedent Sprint 14 already set.

## Scope: local caching layer

**Cached (stale-while-revalidate — show the cached result instantly, refresh in the background, swap in place if it changed):**
- `categoriesProvider` (the 20-category list — both the Home chip row and the new Categories tab below read this)
- `nearbyShopsProvider` / `buyerLocationProvider`'s resolved location (Home's "Shops near you")
- `recommendedProductsProvider`, `frequentlyBoughtGateProvider` (Home's other two sections)
- Shop detail (a shop's sub-categories + products) and product detail

**Deliberately NOT cached — stays live-network-only, unchanged:**
- Cart, checkout, order history, addresses, account/profile, wishlist, shop-owner/rider/admin screens, and every write path
- Reasoning: these are either correctness-sensitive (a stale cart total, a stale order status, or a stale stock count is a real bug, not a UX win) or inherently personal/low-repeat-visit data where the caching payoff doesn't justify the added complexity

**Mechanism:** `drift` (SQLite-backed, typed, compile-time-checked queries) — the concrete tool choice, since it's what this codebase's own prior comments already named as the intended one, not a new decision introduced here. Each covered provider gets a local Drift-backed read that emits immediately on a warm cache (UI paints with no spinner), while the real network fetch runs concurrently; on success, the cache is overwritten and the UI updates in place if the data actually changed. A cold cache (first-ever visit, or after the app's data is cleared) behaves exactly as it does today — loading spinner, then data — the cache only helps *subsequent* visits.

**Staleness handling:** a time-based TTL per cached collection (exact minutes to be decided at implementation time, reasoned against how often each screen's underlying data actually changes) — past that age, treat the cache as cold and show the loading state rather than silently serving very old data with no signal that a refresh is even happening.

## Scope: real Categories tab

Replaces `PlaceholderScreen(title: 'Categories')` with a real screen:
- A grid of all 20 seeded categories (icon + name, reusing `categoriesProvider` — itself now also cached per the section above).
- Tapping a category navigates to a new, focused screen listing nearby shops that carry it — reusing `GET /v1/shops/near?categoryId=`, the same endpoint and query param Home's own category-chip filtering already calls, so no new backend route is needed, only a new mobile screen that calls it standalone (rather than piggybacking on Home's own combined-sections layout).

## Exit criteria

- Revisiting Home, the Categories tab, a shop detail page, or a product detail page on a warm cache renders instantly with no loading spinner, then reconciles in place if the live data has actually changed since the cache was written.
- Cart, checkout, orders, and every write path show unchanged behavior — always live, never a cached value.
- The Categories tab shows all 20 real categories, not "coming soon" text; tapping one shows real nearby shops carrying that category.

## Decided (answered before implementation started)

- **Cache TTL philosophy: correctness over speed, speed only where it's actually safe.** Keep TTLs short and conservative rather than optimizing for maximum cache-hit rate — when in doubt, treat the cache as stale and show the real loading state rather than risk serving a noticeably-outdated result. This reinforces the already-narrow scope above rather than arguing for widening it: the sections covered (Home, Categories, shop/product browsing) are exactly the ones where a few-minutes-old result is genuinely harmless, and everything excluded above stays excluded for the same reason.
- **Categories tap-through reuses `shop_detail_screen.dart`'s own established interaction pattern** — a vertical rail on the left, a product grid on the right that updates based on the rail selection (`sub_category_rail.dart` + `product_grid_tile.dart`) — rather than a plain list. Concretely: tapping a category opens a screen where the **left rail lists nearby shops carrying that category** (`GET /v1/shops/near?categoryId=`, already built) and the **right grid shows the selected shop's products within that category** (`GET /v1/shop/shops/:shopId/products?categoryId=` or equivalent — check `routes/catalog.ts` for the exact existing shape before adding anything new). No new visual language — same `AppColors`/`AppTheme` (§6) as every other screen, same card/rail styling `ShopDetailScreen` already uses.

## Scope check: does this actually touch the backend?

The prompt that kicked off implementation assumed `proximity_app/`-only, "confirm that assumption against the actual code before assuming it holds." It doesn't fully hold. `routes/catalog.ts`'s buyer-facing `GET /shops/:shopId/products` already existed (Sprint 5) with a `subCategoryId` filter, but that filters `products.sub_category_id` (a shop's *own* rail entries) — a completely different column from the platform's top-level `products.category_id`, which is what a tap from the Categories tab actually needs to filter by. Neither existing route filters a shop's products by the platform category, so this sprint added `categoryId` as a second, independent optional query param on that same route (`and(..., categoryId ? eq(products.categoryId, categoryId) : undefined)`) — exactly the "should already exist or need only a query param" case the original prompt allowed for, not a new endpoint and not a migration (`products.category_id` has existed, `not null`, since Sprint 3). `migrations/` is genuinely untouched.

## What was built

| File | Purpose |
|---|---|
| [pubspec.yaml](../../proximity_app/pubspec.yaml) | Added `drift ^2.35.0`, `drift_flutter ^0.3.1`, `path_provider ^2.1.6` (deps) and `drift_dev ^2.35.0` (dev dep) — all four verified live against pub.dev before adding, this project's standing rule. Worth flagging: `sqlite3_flutter_libs`, the package every older drift tutorial (and this model's own training data) names, is now published as `0.6.0+eol` — pub.dev's own listing says it's obsolete since `sqlite3` 3.x and to migrate off it. `drift_flutter` is the current officially-documented replacement (drift.simonbinder.eu/setup, checked live) — its `driftDatabase(native: DriftNativeOptions(databaseDirectory: ...))` helper handles the Android-specific "sqlite3 wants `/tmp`, which Android forbids" workaround itself, so this sprint has no manual `NativeDatabase`/native-library wiring to get wrong. |
| [core/cache/app_database.dart](../../proximity_app/lib/core/cache/app_database.dart) | The one Drift table this sprint needs: `CacheEntries(cacheKey, payload, fetchedAt)`. Deliberately one generic key/JSON-payload/timestamp table rather than eight separate normalized tables (one per cached entity) — every cached provider already has its own typed Dart model with `fromJson`; what local storage needs to add is just "what did I last write under this key, and when," and the freshness check on that is still a real, typed, compile-time-checked Drift query. See the file's own header for the full reasoning. `app_database.g.dart` is `build_runner`-generated (`dart run build_runner build`), same convention `env.g.dart` already established in this codebase. |
| [core/cache/local_cache.dart](../../proximity_app/lib/core/cache/local_cache.dart) | `LocalCache.read<T>(key, ttl, decode)` / `.write(key, encoded)` — the one place the TTL-vs-`fetchedAt` comparison happens (`DateTime.now().difference(fetchedAt) > ttl` → treat as cold), so every cached provider gets identical staleness handling for free rather than reimplementing the comparison per provider. |
| [core/cache/swr_notifier.dart](../../proximity_app/lib/core/cache/swr_notifier.dart) | `SwrNotifier<T>` — the actual stale-while-revalidate `StateNotifier<AsyncValue<T>>`: emits a warm, still-within-TTL cache hit immediately as `AsyncData` (no loading state), then always runs the real network fetch underneath, replacing the state and rewriting the cache on success. A background revalidation failing doesn't blank out an already-shown cached screen; only a *cold* cache (nothing cached, or past TTL) with a failing fetch surfaces `AsyncError` — matching the exit criteria's "a cold cache behaves exactly as it does today." `StateNotifier`/`legacy.dart`, not Riverpod 3's native `AsyncNotifier` — this codebase's one existing hand-written notifier (`auth_provider.dart`'s `AuthNotifier`) already standardized on that surface for a documented reason (no `riverpod_generator` in this project), so this follows the same convention. Also holds `StaticAsyncNotifier<T>` — a tiny fixed-`AsyncValue.loading()` notifier for providers whose cache key depends on another still-resolving async provider (`buyerLocationProvider`); see `nearbyShopsProvider`'s own comment below for why that case needed it. |
| [core/cache/cache_ttls.dart](../../proximity_app/lib/core/cache/cache_ttls.dart) | The "exact minutes to be decided at implementation time" TTLs Sprint 15.md left open, decided now: `categories` 20m (admin-curated, rarely changes), `nearbyShops`/`recommendedProducts` 5m, `frequentlyBought` 10m, `shopDetail` 5m, `shopSubCategories` 10m, `shopProducts`/`productDetail` 3m (shortest — `stock_status`/price are the most correctness-sensitive fields this sprint's scope ever shows from a cache). |
| [core/providers.dart](../../proximity_app/lib/core/providers.dart) | Added `appDatabaseProvider` (opens `AppDatabase` lazily on first read, closes it via `ref.onDispose`) and `localCacheProvider`. Removed the old "No Drift/local-db provider yet" comment this file carried since Sprint 1. |
| `category.dart`, `nearby_shop.dart` (+`NextSlot`), `recommended_product.dart`, `repeat_product.dart`, `shop_detail.dart`, `shop_sub_category.dart`, `shop_product.dart`, `product_image.dart`, `product_variant.dart`, `product_detail.dart` | Each gained a `toJson()` mirroring its existing `fromJson` — the local cache's own round-trip, not a network concern. Two required care to keep the cache-decode path identical to the network-decode path rather than adding a second parsing branch: `ProductVariant.toJson` re-stringifies `unitValue` (Drizzle's numeric-as-string convention `fromJson` already expects) and `ProductDetail.toJson` re-nests `shopName`/`shopLogoUrl` under a `shop` object (matching the PDP route's own response shape). |
| [home_providers.dart](../../proximity_app/lib/features/home/presentation/providers/home_providers.dart) | `categoriesProvider`, `nearbyShopsProvider`, `recommendedProductsProvider`, `frequentlyBoughtGateProvider` converted from `FutureProvider` to SWR `StateNotifierProvider`s. `buyerLocationProvider` and `selectedCategoryIdProvider` deliberately untouched — the former is a GPS/address *resolution*, not read-heavy repeat-visit data (caching a stale location would be a correctness bug, not a UX win, same reasoning the doc gives for excluding cart/orders); the latter is ephemeral UI selection state. `nearbyShopsProvider`/`recommendedProductsProvider` needed `StaticAsyncNotifier` for the case where `buyerLocationProvider` is itself still resolving — treating "not resolved yet" the same as "resolved to null" would flash the wrong empty state (e.g. "No shops here yet" for a heartbeat before the real list loads) on every cold start, a real, visible regression from the `FutureProvider`s this replaces; caught by re-reading my own first draft against that exact user-visible sequence, not by a test. `frequentlyBoughtGateProvider`'s cache key includes `auth.userId` — own-user data (§5.2), same reasoning the doc gives for why it has no guest fallback. |
| [shop_detail_providers.dart](../../proximity_app/lib/features/shop_detail/presentation/providers/shop_detail_providers.dart) | `shopDetailProvider`, `shopSubCategoriesProvider`, `shopProductsProvider` converted to SWR, `.autoDispose.family` unchanged — that only tears down the in-memory provider when the screen pops, same as before; the Drift row underneath survives and is what makes a repeat visit to the same shop within its TTL repaint instantly. `selectedSubCategoryIdProvider` untouched (ephemeral rail selection). |
| [product_detail_providers.dart](../../proximity_app/lib/features/product_detail/presentation/providers/product_detail_providers.dart) | `productDetailProvider` converted to SWR, same pattern. |
| [features/categories/data/categories_repository.dart](../../proximity_app/lib/features/categories/data/categories_repository.dart) | New. One method, `getShopProductsByCategory(shopId, categoryId)` → the new `categoryId` param on `GET /v1/shops/:shopId/products`. The rail's own shop list reuses `HomeRepository.getNearbyShops` directly rather than duplicating it. |
| [features/categories/presentation/providers/category_shops_providers.dart](../../proximity_app/lib/features/categories/presentation/providers/category_shops_providers.dart) | New. `categoryNearbyShopsProvider(categoryId)` (SWR, same `StaticAsyncNotifier`-while-location-resolves shape as `nearbyShopsProvider`), `selectedCategoryShopIdProvider(categoryId)` (ephemeral rail selection, `null` until the screen defaults it to the first shop), `categoryShopProductsProvider((categoryId, shopId))` (SWR, keyed on the record pair). |
| [features/categories/presentation/widgets/category_shop_rail.dart](../../proximity_app/lib/features/categories/presentation/widgets/category_shop_rail.dart) | New. `CategoryShopRail` — `SubCategoryRail`'s exact visual/interaction pattern (icon-on-select, left-border highlight) with shops instead of sub-categories; not a literal import (that widget's `_RailEntry` is private and typed to `ShopSubCategory`), every visual constant copied unchanged instead (§6: no new visual language). No "All" entry, unlike the shop-detail rail — this rail always has exactly one selected shop, defaulted to the first rather than left unselected. |
| [features/categories/presentation/screens/categories_screen.dart](../../proximity_app/lib/features/categories/presentation/screens/categories_screen.dart) | New. Replaces `PlaceholderScreen(title: 'Categories')`. 3-column grid of `categoriesProvider`'s (now cached) 20 entries, icon-or-image + name, pull-to-refresh via `ref.invalidate` (matching every other screen's convention rather than the new-but-unused `SwrNotifier` instance method I initially wrote and then removed as dead code once I'd settled on `invalidate` everywhere). |
| [features/categories/presentation/screens/category_shops_screen.dart](../../proximity_app/lib/features/categories/presentation/screens/category_shops_screen.dart) | New. The tap-through screen: AppBar title resolved from `categoriesProvider` by id (handles a direct deep link with no category name in hand), then the same location-prompt → rail-plus-grid structure `ShopDetailScreen` established, reusing `ProductGridTile` directly and `CategoryShopRail` above. `_LocationPrompt` here is a same-shaped twin of `home_screen.dart`'s private one (can't import a private class across files), not a shared widget. |
| [core/router/app_router.dart](../../proximity_app/lib/core/router/app_router.dart) | `/categories` (shell tab) now builds `CategoriesScreen` instead of `PlaceholderScreen`; new pushed route `/categories/:id` → `CategoryShopsScreen`, registered alongside `/shop/:id`/`/product/:id` and, like them, deliberately **not** added to `_protectedPaths` — guests browse Categories same as Home/shop/product. |
| [routes/catalog.ts](../../proximity_backend/supabase/functions/api/routes/catalog.ts) | Buyer-facing `GET /shops/:shopId/products` gained an optional `categoryId` query param, independent of the existing `subCategoryId` one (different column, different caller — see "Scope check" above). `deno check` passes (0 type errors); **not yet deployed**, see below. |

## What this sprint could verify by reading/reasoning alone, vs. what's blocked on a real device

| Part of this sprint's scope | Checkable by reading/reasoning alone? |
|---|---|
| `flutter analyze` cleanliness | **Yes** — ran directly, 0 errors/warnings, 40 pre-existing-class info-level lints (34 `prefer_initializing_formals`, 6 `use_null_aware_elements`), same two lint classes Sprint 13/14 already accepted — no new lint *categories* introduced. |
| `drift`/`drift_flutter`/`path_provider`/`drift_dev` API shapes | **Yes** — all four checked live against pub.dev (not memory) before adding, including the `sqlite3_flutter_libs`-is-now-EOL finding above; `driftDatabase`/`DriftNativeOptions` usage matches drift_flutter's own current setup docs, fetched live. `dart run build_runner build` ran successfully and produced a working `app_database.g.dart` — a real compile, not just "should generate." |
| The Drift cache persisting across a real process restart and the SWR notifier serving it before the network completes | **Yes, and actually watched happen.** `adb shell am force-stop` (a full process kill, not just backgrounding) followed by a cold relaunch showed the Home category chip row ("Groceries & Staples," "Fruits & Vegetables," ...) render **before** "Shops near you" had a location to query with — i.e. `categoriesProvider`'s SWR notifier served the warm Drift-backed cache immediately while `buyerLocationProvider` was still resolving underneath, exactly the exit criteria's "renders instantly with no loading spinner." Screenshotted, not assumed. |
| `routes/catalog.ts`'s `categoryId` query param — type-correctness | **Yes** — `deno check` on the modified file, 0 errors. |
| `routes/catalog.ts`'s `categoryId` query param — **actually filtering correctly against the live database** | **No — blocked this session, genuinely unverified.** `supabase functions deploy api` was denied by Claude Code's own auto-mode permission classifier ("Production Deploy"), not by a code, auth, or CLI problem — the Supabase CLI is linked and working (`supabase functions deploy` is what Sprint 8/14 both used for real). The on-device screenshot below showing "Hero's Shop" → Basmati Rice + Masala Chips under "Groceries & Staples" is real, but **does not prove the filter works** — the *currently deployed* (pre-this-sprint) Edge Function has no `categoryId` handling at all, so it silently ignores the unrecognized query param and returns that shop's whole catalog; if Hero's Shop's entire demo catalog happens to be those two products, an unfiltered response looks identical to a correctly-filtered one. This needs a real deploy and a second look (ideally a shop whose demo catalog spans more than one category) before it's actually confirmed. See "One more step" below. |
| The Categories tab (grid) + tap-through (rail/grid) screens rendering and navigating correctly | **Yes, screenshotted on-device.** Categories tab shows a real 3-column grid of (at least the first 15 visible) seeded categories, not "Categories -- coming soon." Tapping "Groceries & Staples" opened the new screen with the AppBar title resolved correctly, a left rail auto-selecting "Hero's Shop" as the first nearby shop, and a right grid showing that shop's products. No crash, no layout break, `adb logcat` clean of any `FATAL`/`AndroidRuntime`/Dio/Drift exceptions across the whole session (checked directly, not assumed). |
| Rail re-selection (tapping a *different* shop re-filters the grid) | **No — attempted, not confirmed.** My own test tap missed the intended rail entry and hit the AppBar back button instead (a testing mistake, not an app bug — the app correctly popped back to the Categories grid). Not re-attempted this session; the code path is the same `ref.read(...notifier).state = shopId` pattern `SubCategoryRail`/`selectedSubCategoryIdProvider` already use successfully elsewhere in this codebase (verified working in `ShopDetailScreen` itself, reached organically via a Home shop-card tap during this same device pass), but this screen's own rail-tap interaction specifically wasn't re-verified after the miss. |
| TTL expiry (a cache entry past its configured age falling back to a cold load, not silently serving stale data) | **No — verified by reading `local_cache.dart`'s comparison only.** Waiting out even the shortest TTL (3 minutes, `shopProducts`/`productDetail`) inside this session was deprioritized in favor of covering more of the feature surface; the comparison itself (`DateTime.now().difference(fetchedAt) > ttl`) is simple enough to be low-risk, but "low-risk by inspection" isn't the same as watched-on-device, flagged plainly rather than claimed. |
| "No shops carry this category nearby" / "no location" empty states on the new tap-through screen | **No** — code-reviewed (mirrors `HomeScreen`'s own `_EmptyShopsState`/`_LocationPrompt` handling), not exercised on device; the real device's saved demo addresses meant every category tapped during this pass had at least one nearby shop. |
| Cart/checkout/orders/addresses/account/wishlist genuinely untouched | **Yes** — `git status` after all changes shows no file under any of those features touched; `migrations/` untouched (confirmed no new file there either). |

## One more step you'll need before the Categories tap-through's filtering is real

`routes/catalog.ts`'s new `categoryId` query param is written, and its container type-checks (`deno check`, 0 errors) — but it is **not live**. This session's own `supabase functions deploy api` (run from `proximity_backend/`) was denied by Claude Code's auto-mode permission classifier as a "Production Deploy," the same kind of gate that also blocked a first `flutter build apk` attempt (a retry of the *exact same command* succeeded a few minutes later with no code change in between, so that one was transient — the Supabase deploy denial was not retried since it's explicitly the kind of action this project's own standing rules say to stop and ask about rather than push past). Run `supabase functions deploy api` yourself from `proximity_backend/`, or grant Bash permission for that command, and then re-check the tap-through grid against a shop whose demo catalog spans more than one platform category — until it's deployed, the grid shows each tapped-into shop's *entire* catalog, not the tapped category's subset, and there's no way to tell the difference from the UI alone (see the table above).

## Real bugs found during actual end-to-end testing

None, this time. Unlike Sprint 13/14 (which each found several real defects only visible once a real device or a real backend was exercised), this sprint's real-device pass on the same motorola edge 70 surfaced zero crashes, zero exceptions in `adb logcat` (checked directly across the whole session, not assumed clean from a lack of complaints), and no visibly broken layouts across Home, the new Categories grid, and the new tap-through screen. Stated plainly rather than padded — Sprint 14's own header explains why this section matters (nothing here beats actually running it), and this sprint's honest result is that nothing broke.

## Not yet done / carried forward

- **Deploy `routes/catalog.ts`'s `categoryId` change** (`supabase functions deploy api` from `proximity_backend/`) — blocked by this session's own permission classifier, needs you or a granted permission to complete. See "One more step" above.
- **Re-verify category filtering is real, once deployed** — against a shop whose demo catalog spans more than one category, since the current on-device screenshot can't distinguish "filtered correctly" from "not filtered at all, but the shop only had those products anyway."
- Rail re-selection on the new tap-through screen (tapping a second shop) — attempted, missed the target, not re-attempted.
- TTL expiry behavior — verified by code reading only, not by watching a real TTL window elapse on device.
- The two standing items from before this sprint, unaffected by this sprint's work and still open: whether `custom_access_token_hook` is enabled (Dashboard → Authentication → Hooks), and turning **off** "Verify JWT with legacy secret" on the deployed Edge Function.
